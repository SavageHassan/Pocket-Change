# secrets
.env
.env.*
!.env.example
*.pem
*.key

# dependencies
node_modules/

# build artifacts
dist/
build/
*.tsbuildinfo

# logs
logs/
*.log
npm-debug.log*

# local databases / state
*.sqlite
*.sqlite3
*.db
data/

# M3: devnet pool config is environment-specific (holds this machine's
# generated mint/pool addresses), not secret but not portable — regenerate
# with `npm run setup:devnet-pool` rather than committing it.
devnet-pool.json

# OS / editor
.DS_Store
.vscode/
.idea/
